  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })<  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })>  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })<  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })c  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })=  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })-  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })-  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })>  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })<  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })v  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })-  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })f  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })=  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }):  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })k  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })y  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })=  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }).  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })d  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })|  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })|  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }).  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })c  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })=  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })-  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })-  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })=  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })b  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })b  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })d  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })x  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })=  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })0  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }):  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })-  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })d  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })b  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })d  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })=  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })!  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }).  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })d  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })@  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })c  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })c  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })k  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })=  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })D  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })(  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }))  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })@  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })k  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })y  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }).  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })=  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })D  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })(  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }))  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })@  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })k  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })y  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }).  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })c  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }).  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })v  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })=  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })D  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })(  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }))  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })>  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })<  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })H  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })E  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }):  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })=  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })>  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })<  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })#  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })c  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })>  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })<  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })=  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })c  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }):  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })=  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })>  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })<  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })/  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })>  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })<  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })/  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })>  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })<  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })/  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })H  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })E  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })>  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })<  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })/  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })>  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })<  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })/  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })>  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })<  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })/  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })>  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })<  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })c  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })g  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })=  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })"  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })>  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }){  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })R  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })}  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })f  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })'  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })v  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })-  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })'  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })H  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })E  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })f  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })'  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })@  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })/  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })c  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })/  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })H  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })E  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }).  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })v  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })'  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })y  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }){  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })P  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })y  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })D  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })}  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })f  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })m  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })'  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })y  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })/  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })P  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })y  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })D  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })'  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })d  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })f  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })P  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })<  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }){  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }):  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })P  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })y  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })D  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })[  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })]  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })}  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })>  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })(  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }))  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })c  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })=  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })R  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })(  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }))  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })f  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })c  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })D  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })(  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }):  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })P  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })y  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })D  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }))  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }){  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })|  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })|  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })(  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })l  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })y  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }))  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }).  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })d  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })f  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })(  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })!  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })d  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }))  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })n  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }).  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })u  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })(  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }){  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })a  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })h  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }):  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })/  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })o  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })e  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })y  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })/  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })   const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })}  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ }))  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })}  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })<  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })/  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })s  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })c  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })r  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })i  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })p  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })t  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })>  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
  const rawUuid = hotel.uuid ?? (hotel as any).uuid\n  if (!rawUuid) return\n  const uuid = typeof rawUuid === 'string' ? rawUuid : String(rawUuid)\n  router.push({ path: /property/ })
